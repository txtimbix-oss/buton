# ── API ────────────────────────────────────────────────────────
# В dev: пусто — запросы к /api/* проксируются через vite.config.ts → localhost:3001
# В проде: указать полный URL API-сервера (без trailing slash)
# Пример: VITE_API_BASE=https://api.spbapp.ru
VITE_API_BASE=
